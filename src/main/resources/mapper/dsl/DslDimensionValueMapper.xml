<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.firstaidemo.mapper.dsl.DslDimensionValueMapper">
    <select id="selectByDimensionCode" resultType="com.example.firstaidemo.semanticdsl.metadata.entity.DslDimensionValue">
        SELECT id, dimension_code, value_code, value_name, physical_value, description,
               is_deleted, creator, created_dt, last_editor, last_edited_dt
        FROM agent.dsl_dimension_value WHERE dimension_code = #{dimensionCode} AND is_deleted = false
    </select>
    <select id="selectByDimensionCodes" resultType="com.example.firstaidemo.semanticdsl.metadata.entity.DslDimensionValue">
        SELECT id, dimension_code, value_code, value_name, physical_value, description,
               is_deleted, creator, created_dt, last_editor, last_edited_dt
        FROM agent.dsl_dimension_value WHERE is_deleted = false AND dimension_code IN
        <foreach collection="dimensionCodes" item="code" open="(" separator="," close=")">#{code}</foreach>
    </select>
</mapper>
